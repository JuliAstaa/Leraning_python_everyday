#access list in py using no index
# ex: i want to print banana from list fruits = ['cocoa', 'banana', 'orange'], we can use no index, index start from 0, so to print banana  we do fruits[1], because banana at index 1

fruits = ['cocoa', 'banana', 'orange']
print(fruits[1])